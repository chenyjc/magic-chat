name: Deploy Application

on:
  push:
    branches: [main]
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: npm run build

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata for Docker
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{date 'YYYYMMDD'}}-
          type=raw,value=latest,onlatest=true

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # Optional: Deploy to your favorite platform
    # Below are some examples:

    # Example: Deploy to Azure Container Instances
    # - name: Deploy to Azure Container Instances
    #   uses: azure/aci-deploy@v1
    #   with:
    #     resource-group: my-resource-group
    #     dns-name-label: magic-chat-${{ github.run_number }}
    #     image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
    #     registry-login-server: ${{ env.REGISTRY }}
    #     registry-username: ${{ github.actor }}
    #     registry-password: ${{ secrets.GITHUB_TOKEN }}
    #     name: magic-chat-app
    #     location: eastus
    #     ports: '3000'

    # Example: Deploy to AWS ECS
    # - name: Configure AWS credentials
    #   uses: aws-actions/configure-aws-credentials@v1
    #   with:
    #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    #     aws-region: us-east-1

    # Example: Deploy to Google Cloud Run
    # - name: Setup gcloud CLI
    #   uses: google-github-actions/setup-gcloud@v0
    #   with:
    #     service_account_key: ${{ secrets.GCP_SA_KEY }}
    #     project_id: ${{ secrets.GCP_PROJECT_ID }}

    # - name: Deploy to Cloud Run
    #   run: |
    #     gcloud run deploy magic-chat \
    #       --image ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
    #       --platform managed \
    #       --region us-central1 \
    #       --allow-unauthenticated \
    #       --port 3000 \
    #       --set-env-vars OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }},OLLAMA_BASE_URL=${{ secrets.OLLAMA_BASE_URL }},OLLAMA_MODEL=${{ secrets.OLLAMA_MODEL }}